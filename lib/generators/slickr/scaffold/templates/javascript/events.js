document.addEventListener('DOMContentLoaded', () => {
  const el = document.getElementById('events_listing');

  if (el != null) {
    document.addEventListener('ResultsFiltered', (event) => {
    })
  }
})
