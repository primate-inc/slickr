document.addEventListener('DOMContentLoaded', () => {
  const el = document.getElementById('news_listing');

  if (el != null) {
    document.addEventListener('ResultsFiltered', (event) => {
    })
  }
})
